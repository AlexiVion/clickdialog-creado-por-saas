---
import DashboardLayout from "../../layouts/DashboardLayout.astro";
---
<DashboardLayout title="Landings · ClickDialog">
  <div class="card">
    <h1 style="margin:0 0 8px 0;">Landings</h1>
    <p class="muted" style="margin:0 0 14px 0;">Lista simple desde el backend.</p>
    <button id="btn" class="btn primary" type="button">Cargar</button>
    <pre id="out" style="margin-top: 12px; white-space: pre-wrap;"></pre>
  </div>

  <script>
    const btn = document.getElementById("btn");
    const out = document.getElementById("out");
    btn.addEventListener("click", async () => {
      out.textContent = "Cargando...";
      const res = await fetch("http://localhost:5005/api/landingpages");
      const j = await res.json();
      out.textContent = JSON.stringify(j, null, 2);
    });
  </script>
</DashboardLayout>
